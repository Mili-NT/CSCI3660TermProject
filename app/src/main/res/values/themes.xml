<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CosmicCross" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.CosmicCross" parent="Base.Theme.CosmicCross" />

    <!-- Main font style -->
    <style name="primaryLabel">
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/honeypirls</item>
    </style>

    <!-- Theme of chip buttons -->
    <style name="AppTheme.ChipStyle" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/purple</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/honeypirls</item>
    </style>

</resources>
